/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // NEXT_PUBLIC_* variables are automatically available in Next.js
  // No need to declare them in env section
}

module.exports = nextConfig

